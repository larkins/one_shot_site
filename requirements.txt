# Core dependencies
requests>=2.28.0
web3>=6.0.0
eth-account>=0.5.0
python-dotenv>=1.0.0

# Optional for browser automation (cloudflare_auto.py)
# playwright>=1.40.0  # Uncomment if using cloudflare_auto.py

# Database (if using direct DB queries)
# psycopg2-binary>=2.9.0  # Uncomment if using PostgreSQL directly

# YAML parsing
pyyaml>=6.0